---
"@konsistent/convention": patch
"konsistent": patch
---

feat(konsistent): split `importFrom` into `importValuesFrom` and `importTypesFrom` and rename existing bare import predicates to their `importValues*` equivalents for clarity and consistency
